---
description: Shared frontend with ARPU计算 — canonical repo + symlink mirror
globs: app.js,index.html,styles.css,scripts/sync-arpu-mirror.sh
alwaysApply: false
---

# 与 ARPU计算 共用前端文件

**唯一源码目录**：本仓库 `Calculation-of-winning-probability/`。

**镜像**（`../ARPU计算/` 下应为符号链接，勿在 ARPU 侧维护独立副本）：

| 文件 | 说明 |
|------|------|
| `app.js` | 计算与表格逻辑 |
| `index.html` | 页面与指标口径说明 |
| `styles.css` | 样式 |

**恢复 / 初次对齐镜像**（在本仓库根目录）：

```bash
./scripts/sync-arpu-mirror.sh
```

若 ARPU 目录里还是**普通文件**而非链接，脚本会跳过并提示；要改成链接并覆盖本地文件：

```bash
FORCE=1 ./scripts/sync-arpu-mirror.sh
```

旧脚本名 `./scripts/sync-app-js-to-arpu.sh` 与上面等价。

**业务规则简述**：无 `holdback` → 基准为 **control**；有 `holdback` → 基准为 **holdback**，其余组（含 control）均与其对比。

**协作注意**：Git 会记录 symlink；他人克隆后若路径不是 `cursor/ARPU计算` 与 `cursor/Calculation-of-winning-probability` 并列，需在本地改相对路径或重新执行 `FORCE=1 ./scripts/sync-arpu-mirror.sh`。
